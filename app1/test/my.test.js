const assert = require('assert');
describe('Simple Math Test', () => {
 it('1 plus 1 is 2', () => {
        assert.equal(1 + 1, 2);
    });
 it('3 by 3 is 9', () => {
        assert.equal(3 * 3, 9);
    });
});

